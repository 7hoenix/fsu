<%= form_for [@lecture, @schedule] do |f| %>
  <% if @schedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@schedule.errors.count, "error") %> prohibited this schedule from being saved:</h2>

      <ul>
      <% @schedule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <%= f.hidden_field :lecture_id, :value => @lecture.id %>

  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :day %>
  </div>

  <div class="field">
    <%= f.label :time %><br>
    <%= f.time_select :start %>
  </div>
  
  <div class="field">
    <%= f.label :duration %><br>
    <%= f.select :duration, [["30 minutes", 30],["45 minutes", 45],["1 hour", 60],["2 Hours", 120]] %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
